{
  "name": "aqi",
  "version": "1.0.0",
  "description": "Air Quality Index Project. This will run both frontend and backend servers concurrently. To run separately : 'cd backed && npm start' and 'cd frontend && npm run dev'",
  "scripts": {
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
